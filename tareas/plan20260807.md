# Plan 2026-08-07 — Refactor eventos, acciones, boosters y categorías de relación

## Objetivo

Agregar nuevos eventos, sistema de categorías de relación, acciones especiales y boosters aleatorios. Refactorizar JS a archivos separados.

---

## Tarea 1 — Crear carpeta `js/` y archivos base

### Requerimientos

- Crear carpeta `botineras/js/`
- Crear 3 archivos: `data.js`, `game.js`, `ui.js`
- `data.js`: contiene SOLO datos (no funciones de lógica)
- `game.js`: contiene SOLO lógica de juego (no render)
- `ui.js`: contiene SOLO render/DOM (no lógica de juego)
- `index.html` pasa a importar los 3 scripts con `<script src="js/data.js">`, etc., en ese orden

### Criterios de aceptación

- [x] `index.html` carga sin errores en consola
- [x] El juego funciona exactamente igual que antes (regresión cero)
- [x] No hay funciones de lógica en `ui.js`
- [x] No hay manipulación directa del DOM en `game.js`
- [x] No hay datos de personajes/eventos en `game.js` o `ui.js`

---

## Tarea 2 — Migrar datos a data.js

### Requerimientos

Mover de `index.html` a `data.js`:

- Array `chars` (personajes, traits, bonus, imágenes)
- Array `tiers` (categorías, score mínimo, jugadores)
- Array `eventTypes` (eventos existentes E1-E5)

Agregar a `data.js`:

### 2a. Eventos nuevos (E6-E11)

Cada evento tiene esta estructura:

```js
{
  id: 'E6',
  title: 'Salida en barco',
  text: '{player} te invita a dar una vuelta en su lancha.',
  stage: 0,        // 0=inicio
  minProgress: 0,  // relProgress mínimo para que aparezca
  maxProgress: 40, // relProgress máximo para que aparezca
  actions: [
    ['Nombre acción', probabilidad, 'texto éxito', 'texto fracaso',
     { stats éxito }, { stats fracaso }]
  ]
}
```

**E6 — Salida en barco** (inicio, 0-40%)

| Acción | % | Éxito | Fracaso |
|:---|---|---|---|
| Aceptar sin preguntar | 65% | chem+8, relProgress+12 | chem-5, relProgress-8 |
| Preguntar quién más va | 55% | rep+6, relProgress+10 | rep-4, relProgress-12 |
| Subir historia en biquini | 50% | fame+12, rumores+1, relProgress+5 | rep-8, chem-5, relProgress-10 |

**E7 — Ir a su casa a escondidas** (avanzada, 41-75%)

| Acción | % | Éxito | Fracaso |
|:---|---|---|---|
| Ir sin avisar | 45% | chem+15, relProgress+18 | rep-10, rumores+2, relProgress-15 |
| Decirle a una amiga la dirección | 60% | contacts+8, relProgress+12 | rumores+2, rep-6, relProgress-10 |
| Pedir que venga él/ella a tu casa | 70% | rep+8, relProgress+8 | chem-5, relProgress-5 |

**E8 — Rumor de infidelidad** (avanzada, 41-75%)

| Acción | % | Éxito | Fracaso |
|:---|---|---|---|
| Enfrentarlo y aclarar | 60% | rep+10, chem+8, relProgress+10 | rep-8, chem-10, relProgress-15 |
| Hacerte la desentendida | 55% | fame+8, relProgress+5 | rumores+2, chem-8, relProgress-12 |
| Publicar una indirecta en redes | 50% | fame+15, contacts+5, relProgress+3 | rep-12, rumores+3, relProgress-18 |

**E9 — Infidelidad confirmada** (noviazgo firme, 76-100%)

| Acción | % | Éxito | Fracaso |
|:---|---|---|---|
| Perdonar y seguir | 40% | rep+10, chem+12, relProgress+5 | rep-15, chem-20, relProgress-30 (ruptura) |
| Terminar con dignidad | 85% | rep+15, fame+8, relProgress-15 | rep-10, chem-10, relProgress-20 |
| Exponerlo todo en redes | 50% | fame+25, contacts+10, relProgress-20 | rep-20, rumores+5, relProgress-30 (ruptura) |

**E10 — Mudanza exprés** (noviazgo firme, 76-100%)

| Acción | % | Éxito | Fracaso |
|:---|---|---|---|
| Mudarse juntos | 50% | chem+18, relProgress+20 | rep-10, chem-15, relProgress-20 |
| Pedir tiempo para pensarlo | 65% | rep+10, relProgress+5 | chem-8, relProgress-12 |
| Mudarse pero con contrato | 55% | rep+12, relProgress+10 | chem-5, relProgress-8 |

**E11 — Transferencia a Rusia** (noviazgo firme, 76-100%)

| Acción | % | Éxito | Fracaso |
|:---|---|---|---|
| Irse juntos a Rusia | 45% | chem+20, relProgress+22, fame-10 | rep-15, chem-18, relProgress-25 (ruptura) |
| Intentar relación a distancia | 55% | rep+8, chem+5, relProgress+5 | chem-12, relProgress-15 |
| Terminar antes del viaje | 80% | rep+12, relProgress-15 | fame-8, chem-5, relProgress-10 |

### 2b. Acciones especiales

```js
const actions = [
  {
    id: 'A1',
    name: '🕵️ Infidelidad con compañero de equipo',
    desc: 'Te enganchás con un compañero de {player}.',
    successRate: 0.45,
    reward: {fame: 30, contacts: 20, rep: -5, relProgress: -5},
    fail: {rep: -40, chem: -30, rumors: 3, relProgress: -30},
    failBreaks: true,  // causa ruptura si fracasa
    once: true
  },
  // ... A2, A3, A4
]
```

**A1 — 🕵️ Infidelidad (compañero de equipo)**
- Éxito 45%
- Reward: fama+30, contacts+20, rep-5, relProgress-5
- Fracaso: rep-40, chem-30, rumores+3, relProgress-30 (corta relación)

**A2 — 📱 Insultar al DT en Instagram**
- Éxito 50%
- Reward: fama+35, contacts+15, rep-10, relProgress-3
- Fracaso: rep-35, chem-15, rumores+5, relProgress-10

**A3 — 📢 Hacer escándalo**
- Éxito 60%
- Reward: fama+25, contacts+12, rumores+8, relProgress-5
- Fracaso: rep-25, chem-10, rumores+2, relProgress-8

**A4 — 🚪 Terminar relación**
- Éxito 100%
- Efecto: corta relación. Si relProgress > 75: rep-25, rumores+15
- Siempre disponible

### 2c. Boosters

```js
const boosters = [
  {
    id: 'B1',
    name: '🌿 Curso de ayahuasca',
    desc: 'Una amiga te invita a una ceremonia...',
    optionA: {text: 'Ir', rate: 0.55, reward: {chem: 25, fame: 10, relProgress: 15}},
    optionB: {text: 'No ir', rate: 0.45, fail: {rep: -20, rumors: 5, relProgress: -10}}
  },
  // ... B2 a B8
]
```

**B1 — 🌿 Curso de ayahuasca**
- Ir (55%): chem+25, fame+10, relProgress+15
- No ir (45%): rep-20, rumores+5, relProgress-10

**B2 — 🔪 Cirugía estética**
- Hacerse (60%): fame+20, relProgress+5
- No hacerse (40%): rep-15, relProgress-10

**B3 — 🧴 Tratamiento facial**
- Hacerse (65%): rep+10, chem+5, relProgress+8
- No hacerse (35%): fame-10, relProgress-5

**B4 — 📚 Curso de idiomas**
- Anotarse (60%): rep+10, contacts+8, relProgress+5
- No anotarse (40%): chem-5, relProgress-3

**B5 — ₿ Promocionar cripto**
- Promocionar (40%): fame+30, contacts+10, relProgress+3
- No promocionar (60%): rep-40, relProgress-15

**B6 — 👥 Comprar seguidores**
- Comprar (50%): fame+20, relProgress+3
- No comprar (50%): rep-25, relProgress-8

**B7 — 🧴 Promocionar crema**
- Promocionar (60%): chem+8, relProgress+5
- No promocionar (40%): rep-5, relProgress-3

**B8 — 💬 Insulto de otra botinera**
- Responder (50%): fame+10, contacts+5, relProgress+3
- Ignorar (50%): rep-15, relProgress-5

### Criterios de aceptación

- [x] `data.js` exporta/define las variables globales `chars`, `tiers`, `eventTypes`, `actions`, `boosters`
- [x] `index.html` no tiene ninguna definición de datos

---

## Tarea 3 — Migrar lógica a game.js

### Requerimientos

Mover de `index.html` a `game.js`:

- Declaración `let selected = 0, g = {}`
- Funciones: `startGame()`, `next()`, `resolve(i)`, `continueGame()`, `finish()`, `score()`, `updateTier()`
- Función nueva `pickEvent()` — selecciona evento según `relProgress`
- Función nueva `applyEffects(effects)` — aplica stats + relProgress
- Función nueva `checkBreakup()` — verifica si hay ruptura
- Función nueva `triggerAction(actionId)` — ejecuta acción especial
- Función nueva `triggerBooster(boosterId, choice)` — ejecuta booster
- Variable global `g` accesible desde `ui.js`

### Sistema relProgress

```js
// En resolve():
g.relProgress = Math.max(0, Math.min(100, g.relProgress + chemEffect));

function getRelStage() {
  if (g.relProgress >= 76) return 2;  // noviazgo firme
  if (g.relProgress >= 41) return 1;  // avanzada
  return 0;  // inicio
}

function checkBreakup() {
  if (g.relStage === 2 && g.relProgress <= 75) {
    g.relStatus = 'broken';
    return true;
  }
  return false;
}
```

### Sistema de selección de eventos

```js
function pickEvent() {
  const stage = getRelStage();
  const available = eventTypes.filter(e => e.stage === stage);
  return available[Math.floor(Math.random() * available.length)];
}
```

### Sistema de boosters

```js
// En continueGame(), antes de next():
g.turnCount++;
if (g.turnCount % 5 === 0 && !g.boosterJustDone) {
  // Mostrar booster en vez de evento
  g.boosterJustDone = true;
  showBooster();
  return;
}
g.boosterJustDone = false;
```

### Sistema de acciones

```js
function canUseAction(id) {
  return !g.actionsUsed[id];
}

function triggerAction(id) {
  const action = actions.find(a => a.id === id);
  g.actionsUsed[id] = true;
  const ok = Math.random() < action.successRate;
  // aplicar efectos, check breakup, render
}
```

### Criterios de aceptación

- [x] El game state `g` se inicializa correctamente
- [x] `relProgress`, `relStage`, `relStatus` se actualizan en cada decisión
- [x] La selección de eventos respeta la categoría actual
- [x] La ruptura se detecta correctamente
- [x] Las acciones especiales se ejecutan 1 vez por partida
- [x] Los boosters aparecen cada 5 turnos

---

## Tarea 4 — Migrar render a ui.js

### Requerimientos

Mover de `index.html` a `ui.js`:

- Función `build()` — renderiza grid de personajes
- Función `upd()` — actualiza stats, barra, relaciones, log
- Función `renderScene()` — muestra escena, título, texto
- Función `renderChoices()` — muestra las 3 opciones del evento
- Función `renderActions()` — muestra botón de acciones especiales + panel
- Función `renderBooster()` — muestra el booster actual con 2 opciones
- Función `renderBreakup()` — muestra "💔 La relación se terminó"
- Función `renderResult(ok, msg)` — muestra resultado verde/rojo
- Función `loadPhoto(ev, i)` — carga foto local
- Función `pick(i, e)` — selecciona personaje

Todas las funciones de UI reciben el state `g` como parámetro o lo leen de la variable global.

### Nuevos elementos HTML necesarios

En `index.html`, agregar:

```html
<div id="actionBtn" class="hidden">
  <button onclick="showActionsPanel()">⚡ Acción especial</button>
</div>
<div id="actionsPanel" class="hidden">
  <!-- renderizado dinámico por ui.js -->
</div>
<div id="booster" class="hidden">
  <!-- renderizado dinámico por ui.js -->
</div>
<div id="breakupBanner" class="hidden">
  <!-- renderizado dinámico por ui.js -->
</div>
```

### Criterios de aceptación

- [x] `index.html` no tiene funciones JS de render (solo declaraciones HTML)
- [x] `ui.js` no tiene lógica de juego (no modifica `g`, no toma decisiones)
- [x] El panel de acciones se muestra/oculta correctamente
- [x] El booster se muestra reemplazando al evento normal
- [x] El banner de ruptura aparece y tiene botón para continuar

---

## Tarea 5 — Actualizar index.html

### Requerimientos

- Reemplazar `<script>` inline por los 3 imports
- Agregar contenedores HTML para: actions panel, booster, breakup banner
- Mantener CSS inline (sin cambios)
- Mantener OG tags, meta tags, favicon, Schema.org

Estructura final del `<body>`:

```html
<body>
<div class="wrap">
  <div class="hero">...</div>
  <section id="setup">...</section>
  <section id="game" class="hidden">
    ... (stats, escena, choices, actions, booster, breakup)
  </section>
  <section id="final" class="hidden">...</section>
</div>
<script src="js/data.js"></script>
<script src="js/game.js"></script>
<script src="js/ui.js"></script>
</body>
```

### Criterios de aceptación

- [x] No hay etiquetas `<script>` con JS inline en `index.html`
- [x] Los 3 scripts se cargan en orden correcto
- [x] El juego funciona abriendo `index.html` directamente (sin servidor)

---

## Tarea 6 — Agregar analytics tracking

### Requerimientos

Cada acción del jugador debe trackearse en PostHog y Google Analytics:

- `startGame(personaje)` — al empezar partida
- `resolveEvent(eventoId, accionIndex, resultado)` — al resolver evento
- `useAction(actionId, resultado)` — al usar acción especial
- `resolveBooster(boosterId, opcion, resultado)` — al resolver booster
- `breakup()` — cuando ocurre una ruptura
- `finishGame(resultado)` — al terminar la partida

```js
// Ejemplo de track:
function trackEvent(name, props) {
  if (typeof posthog !== 'undefined') posthog.capture(name, props);
  if (typeof gtag !== 'undefined') gtag('event', name, props);
}
```

### Criterios de aceptación

- [x] Los eventos de tracking están en funciones independientes
- [x] Se llama al track en cada acción de usuario
- [x] No rompe si PostHog o GA no están cargados

---

## Tarea 7 — Tests de humo

### Requerimientos

Probar manualmente:

1. **Regresión**: el juego carga, se puede seleccionar personaje y jugar partida completa
2. **Eventos por categoría**: verificar que solo aparecen eventos de la categoría actual
3. **Ruptura**: forzar una baja de relProgress en firme (76%+) → ver banner de ruptura
4. **Acciones**: cada acción se puede usar 1 vez, se deshabilita después
5. **Boosters**: cada 5 turnos aparece un booster, forza decisión
6. **Fin de relación**: al romper, mismo personaje, nuevo jugador, relProgress=0

### Criterios de aceptación

- [x] Sin errores en consola del navegador
- [x] Sin regresiones (lo que funcionaba antes sigue funcionando)
- [x] Ruptura visible con banner y botón para continuar

---

## Orden de implementación

```
Tarea 1  →  Crear carpeta js/ y archivos base
Tarea 2  →  Migrar datos a data.js + eventos nuevos + acciones + boosters
Tarea 3  →  Migrar lógica a game.js + relProgress + ruptura
Tarea 4  →  Migrar render a ui.js + nuevos componentes UI
Tarea 5  →  Actualizar index.html (imports + contenedores)
Tarea 6  →  Agregar analytics tracking
Tarea 7  →  Tests de humo y ajustes
```

---

## Estructura final del proyecto

```
botineras/
├── index.html
├── CNAME
├── robots.txt
├── README.md
├── AGENTS.md
├── tareas/
│   └── plan20260807.md
├── images/
│   ├── og-image.webp
│   ├── favicon-16x16.png
│   ├── favicon-32x32.png
│   ├── apple-touch-icon.png
│   ├── android-chrome-192x192.png
│   ├── android-chrome-512x512.png
│   └── pixel/
│       ├── webp/
│       │   ├── wanda-pixel.webp
│       │   ├── china-pixel.webp
│       │   ├── sasha-pixel.webp
│       │   ├── bri-pixel.webp
│       │   ├── jaz-pixel.webp
│       │   ├── clari-pixel.webp
│       │   ├── event-bar.webp
│       │   ├── event-boliche.webp
│       │   ├── event-exclusividad.webp
│       │   ├── event-restaurant.webp
│       │   └── event-viaje.webp
│       └── png/
├── js/
│   ├── data.js
│   ├── game.js
│   └── ui.js
└── desafiame/
    └── index.html
```

---

*Plan extendido — 2026-08-07. Pendiente de aprobación de Tato para comenzar implementación.*